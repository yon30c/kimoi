class UpdateModel {
  String downloadUrl, versionName, contentText;
  int versionCode, minSupport;

  UpdateModel(
    this.downloadUrl,
    this.versionName,
    this.versionCode,
    this.minSupport,
    this.contentText,
  );
}
